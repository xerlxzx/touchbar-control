## Change

Describe the problem and the resulting behavior.

## Validation

- [ ] `./test.sh`
- [ ] `./build.sh 'work/staged/Touch Bar Control.app'`

For UI changes, include preview screenshots. For hardware-control changes, record the Mac model, macOS version, and observed behavior. Distinguish simulated checks from physical tests; list the checks you haven't run.
